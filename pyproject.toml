[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "harness-connector"
version = "0.1.0"
description = "Plugin-ready collection of independent coding harness session skills"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "ZiChenWang114514"}]

[tool.pytest.ini_options]
testpaths = ["tests"]
