interface:
  display_name: "OpenCode Session"
  short_description: "发现、试跑并调度本机 OpenCode 会话"
  default_prompt: "Use $codex-opencode-session to inspect OpenCode and run this task in the specified workspace."
policy:
  allow_implicit_invocation: true
