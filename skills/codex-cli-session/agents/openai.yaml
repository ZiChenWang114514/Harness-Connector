interface:
  display_name: "Codex CLI Session"
  short_description: "Run and resume local Codex CLI sessions"
  default_prompt: "Use $codex-cli-session to run this task in the specified workspace."
policy:
  allow_implicit_invocation: true
